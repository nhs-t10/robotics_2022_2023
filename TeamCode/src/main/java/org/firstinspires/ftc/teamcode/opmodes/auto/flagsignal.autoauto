#init:
    driveOmni(1, 0, 0), after 13in next;
    let cv_magic = getCVNumberForTesting(), next;
    if(cv_magic == 1) {goto blue} else goto red
    #blue
    //do stuff
    #red
    //do stuff

