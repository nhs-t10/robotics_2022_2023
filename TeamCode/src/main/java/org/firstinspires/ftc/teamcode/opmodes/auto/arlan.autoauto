$
    ignorewarning_american_m_unit: true
$

goto start;

#start:
    startWebcam(), next;
    logger.log(colorFind(0)), next;
    logger.log(colorFind(1)), after 10s next;
    logger.log(colorFind(0)), next;
    logger.log(colorFind(1)), after 10s next;
    logger.log(colorFind(0)), next;
    logger.log(colorFind(1)), after 10s next;
    goto end;




#end:
    stopWebcam();
    log("i got to the end!");
